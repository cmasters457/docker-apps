# docker-apps config - backup

Generic configuration for:

- Postgres (database)
- PGAdmin (sql mgmt)
- Traefik (proxy)
- Maybe (finace app)

Requires two docker networks created, "proxy" & "sql-isolate".